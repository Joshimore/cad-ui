{
  "_comment": "Скопируй этот файл в КОРЕНЬ рабочей папки как workspace.config.json (в изолированном режиме — это сама папка cad-ui). Файл в .gitignore. Всё опционально; блок 'status' — для навыка Discord-статусов, настройка в .claude/skills/status/SETUP.md.",
  "exclude_dirs": ["my-private-folder", "*_cache"],
  "exclude_extensions": [".parquet"],
  "claude_command": "claude",
  "status": {
    "status_webhook":   "https://discord.com/api/webhooks/REPLACE_STATUS_WEBHOOK",
    "summary_webhook":  "https://discord.com/api/webhooks/REPLACE_SUMMARY_WEBHOOK",
    "username":         "Статус",
    "summary_username": "Итоги дня",
    "projects": {
      "example-project": {
        "name":     "Example Project",
        "path":     "Working directory/example-project",
        "webhook":  "https://discord.com/api/webhooks/REPLACE_PROJECT_WEBHOOK",
        "username": "Example Project"
      }
    }
  }
}
